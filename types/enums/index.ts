export enum RoleEnum {
  USER = "user",
  ADMIN = "admin",
  DOCTOR = "doctor",
}

export enum RouteEnum {
  HOME = "/",
  LOGIN = "/login",
  DASHBOARD = "/dashboard",
  PROFILE = "/profile",
  SIGNUP = "/register",
}

export enum ThemeEnum {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system",
}

export enum RegisterTypeEnum {
  PATIENT = "patient",
  DOCTOR = "doctor",
}
