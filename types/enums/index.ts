export enum RoleEnum {
  USER = "user",
  ADMIN = "admin",
  DOCTOR = "doctor",
}

export enum RouteEnum {
  HOME = "/",
  LOGIN = "/login",
  DASHBOARD = "/dashboard",
  PROFILE = "/profile",
}

export enum ThemeEnum {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system",
}
